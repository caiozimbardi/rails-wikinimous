
<div id="list" class="container d-flex justify-content-center">
  <ul>
    <% @articles.each do |article| %>
    <li>
      <h3><%= link_to article.title, article_path(article) %></h3>
      <p><%= article.content %></p>
    </li>
    <% end %>
  </ul>
</div>
